specVersion: "v1"
serviceId: "redlineapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "redlineapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
